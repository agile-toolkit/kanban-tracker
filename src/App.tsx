/**
 * Scaffold placeholder. This app has no GOAL.md yet (see the "Goal needed"
 * issue) — per the suite's process, features aren't built until a human
 * supplies one. Nothing below is a product decision.
 */
export default function App() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6">
      <div className="card max-w-md text-center">
        <h1 className="font-display text-2xl font-semibold mb-2">Kanban Tracker</h1>
        <p className="text-gray-500 dark:text-gray-400">
          Scaffolded, not yet built. Waiting on a goal — see this repo's open
          "Goal needed" issue.
        </p>
      </div>
    </div>
  )
}
