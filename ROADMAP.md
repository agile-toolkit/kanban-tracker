# Kanban Tracker — Roadmap

No `GOAL.md` yet — this file carries only a `## Provisional` section until a
human supplies one. No epics: an epic without a goal is a guess. See the
open "Goal needed" issue.

## Provisional

Nothing yet. The scaffold is clean (fresh Vite/React/TS/Tailwind app, build
and test both pass, no i18n gaps — all four locale files are empty and in
parity, no a11y issues — the only screen is a one-line placeholder). Nothing
here is broken or missing in a goal-independent way; there's simply no
product yet.

## Polish backlog

No small un-filed items queued.

## Shipped

- ~~Scaffold — Vite + React 18 + TypeScript + Tailwind CSS + react-i18next
  (EN/ES/BE/RU stubs), GitHub Pages deploy workflow, design-system tokens~~
